{
  "name": "onyx-monitor-api",
  "version": "1.0.0",
  "description": "Onyx Monitor - API Backend",
  "scripts": {
    "start": "node --import tsx server/index.ts",
    "dev": "tsx watch server/index.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
